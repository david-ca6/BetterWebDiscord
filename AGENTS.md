# Agents

The goal of the extension is not to replace nitro, but to provide automated way to do stuff that people already do manually. 
- for example, instead of downloading an emote as an image to post as an image in a server to show someone, the app allow to post the emote as an image immediately without downloading it first.